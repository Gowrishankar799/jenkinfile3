

/*let str = "Hello world";
console.log(str.length);
console.log(str.toLowerCase());
console.log(str.toUpperCase());
console.log(str.concat(  str));
let lcase = str.toLowerCase();
console.log(lcase);
console.log(str.slice(3,6));
console.log(str.includes("world"));
console.log(str.split(" "));
console.log(str.replace("world","universe"));
console.log(str);*/
/*var isAnagram = function(s, t) {
    if (s.length !== t.length) {
        return false;
    }
    const sortedS = s.split('').sort().join('');
    const sortedT = t.split('').sort().join('');
    return sortedS === sortedT;
};

// Driver code for the isAnagram function
let s1 = "anagram";
let t1 = "nagaram";
let result1 = isAnagram(s1, t1);
console.log(result1); // Output: true

let s2 = "rat";
let t2 = "car";
let result2 = isAnagram(s2, t2);
console.log(result2); */// Output: false
/*let s1 = "var";
let s2 = "rav";
isAnagram(s1,s2);
function isAnagram(s1,s2){
    if(s1.length !== s2.length){
        console.log("false");
    }
    const a = s1.split('').sort();
    console.log(a);
    let str = s1.split('').sort().join('');
    let strr = s2.split('').sort().join('');
    if(str === strr){
        console.log("true");
    }
}*//*var isAnagram = function(s, t) {
    if (s.length !== t.length) {
        return false;
    }
    const sortedS = s.split('').sort().join('');
    const sortedT = t.split('').sort().join('');
    return sortedS === sortedT;
};

// Driver code for the isAnagram function
let s1 = "anagram";
let t1 = "nagaram";
let result1 = isAnagram(s1, t1);
console.log(result1); // Output: true

let s2 = "rat";
let t2 = "car";
let result2 = isAnagram(s2, t2);
console.log(result2); // Output: false
*/


